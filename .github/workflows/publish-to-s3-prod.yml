name: "Publish to prod"

on: [pull_request]

env:
  GITHUB_PR_NUMBER: ${{github.event.pull_request.number}}

jobs:
  tests_e2e:
          
    name: Run end-to-end tests
    runs-on: ubuntu-latest
    steps:

      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
      - name: install dependencies
        run: npm ci
      - name: Run build
        run: npm run build 
      - run: echo "${{ toJson(github) }}" 
      - name: install playwright browsers
        run: npx playwright install --with-deps
      - name: npm run test:e2e
        run: npm run test:e2e
        env:
          DEBUG: pw:api
          CONTENTFUL_DELIVERY_ACCESS_TOKEN: ${{ secrets.CONTENTFUL_DELIVERY_ACCESS_TOKEN }}
          CONTENTFUL_ACCESS_TOKEN: ${{ secrets.CONTENTFUL_ACCESS_TOKEN }}
          CONTENTFUL_PREVIEW_ACCESS_TOKEN: ${{ secrets.CONTENTFUL_PREVIEW_ACCESS_TOKEN }}
          CONTENTFUL_SPACE_ID: ${{ secrets.CONTENTFUL_SPACE_ID }}
      

      - name: Report Status
        uses: ravsamhq/notify-slack-action@v2
        if: always()
        with:
          status: ${{ job.status }}
          # token: ${{ secrets.GITHUB_TOKEN }}
          notification_title: "{workflow} has {status_message}"
          message_format: "{emoji} *{workflow}* {status_message} in <{repo_url}|{repo}>"
          footer: "Linked Repo <{repo_url}|{repo}> | <{run_url}|View Workflow>"
          notify_when: "failure"
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}



  # tests_e2e_netlify_prepare:
  #   name: Wait for deployment on Netlify
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Waiting for Netlify Preview
  #       uses: josephduffy/wait-for-netlify-action@v1
  #       id: wait-for-netflify-preview
  #       with:
  #         site_name: "creative-babka-fecd82"
  #         max_timeout: 180

  # tests_e2e_netlify:
  #   needs: tests_e2e_netlify_prepare
  #   name: Run end-to-end tests on Netlify PR preview
  #   runs-on: ubuntu-latest
  #   timeout-minutes: 5
  #   steps:
  #     - uses: actions/checkout@v3
  #     - uses: actions/setup-node@v3
  #     - name: install dependencies
  #       run: npm ci
  #     - name: install playwright browsers
  #       run: npx playwright install --with-deps
  #     - name: npm run test:e2e
  #       run: npm run test:e2e
  #       env:
  #         PLAYWRIGHT_TEST_BASE_URL: "https://deploy-preview-${{env.GITHUB_PR_NUMBER}}--creative-babka-fecd82.netlify.app"
  #         DEBUG: pw:api